# Disabled: rename to pr-preview.yml to re-enable (uses EAS deploy quota on every PR).
name: PR Preview

on:
  pull_request: {}

jobs:
  deploy:
    type: deploy
    name: Deploy PR Preview

  comment:
    needs: [deploy]
    type: github-comment
