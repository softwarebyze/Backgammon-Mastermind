# frozen_string_literal: true

app_identifier(ENV.fetch("ASC_BUNDLE_ID", "com.backgammonmastermind"))
apple_id(ENV["APPLE_ID"]) if ENV["APPLE_ID"]
team_id("75M38Z9JBF")
itc_team_id(ENV["ITC_TEAM_ID"]) if ENV["ITC_TEAM_ID"]
