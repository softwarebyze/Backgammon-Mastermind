# Move review arrow — before / after

Reference: [backgammon-openings-website](https://github.com/softwarebyze/backgammon-openings-website) `move-animation.tsx` — paths connect **checker stack tops**, not triangle bases.

| Image | Description |
|-------|-------------|
| `before-review-arrow.png` | Review arrow from column base (stack count = 1) |
| `after-review-move1.png` | Arrow from moving checker to landing stack top + per-move scrubber label |

Validated on Expo web (`pnpm web`, localhost:8081). Native re-check with Argent when available.
